## 录屏格式可配置化

给 RecordConfig 添加了 `format` 字段，支持在配置中指定输出视频格式 (webm/mp4/mkv 等)。
- config.rs: 新增 `format` 字段，默认 "webm"；在 `add_comments` 中为 [record] 段添加格式/编码器搭配指南注释
- record/mod.rs: 文件名扩展名从配置读取，CRF 参数对 libx264 也生效
- config_cmds.rs: `--set record.format` 可设置格式
- 注意：注释仅在新生成配置文件时出现，已有配置文件不会自动更新
